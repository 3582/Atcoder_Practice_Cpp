#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main()
{
    long long N;
    cin >> N;
    string S;
    cin >> S;
    long long ans = 0;

    vector<long long> C(N);
    vector<char> char_vec(N);

    for (int i = 0; i < S.length(); i++)
    {
    }

    cout << ans << endl;
    return 0;
}